---
title: "Лабораторная работа №3"
subtitle: "Шифрование гаммированием с конечной гаммой"
author:
  - "Кюнкриков Даниил Саналович, НПИмд-01-24, 1132249574"
institute: "Российский университет дружбы народов (РУДН), Москва, Россия"
lang: ru-RU

format:
  pdf:
    toc: true
    toc-title: "Содержание"
    number-sections: true
    lof: true
    lot: true
    pdf-engine: xelatex
    papersize: a4
    mainfont: "Times New Roman"
    sansfont: "Arial"
    monofont: "Consolas"
    geometry: margin=2cm
    fontsize: 12pt
    fig-pos: "H"
    include-in-header:
      text: |
        \usepackage{float}
        \KOMAoption{captions}{tableheading}
    documentclass: scrartcl
    classoption:
      - DIV=11
      - numbers=noendperiod
  docx:
    toc: true
    number-sections: true
---

# Введение

Гаммирование — способ шифрования, при котором символы открытого текста преобразуются с использованием **гаммы** (ключевой последовательности). 
В данной лабораторной работе рассматривается гаммирование в конечном алфавите (русский алфавит из 33 букв), где операции выполняются по модулю *n*.

# Цель и задачи работы

**Цель:** реализовать алгоритм шифрования гаммированием с конечной гаммой на языке Julia.

**Задачи:**

1. Реализовать режимы шифрования и расшифровки.
2. Реализовать циклическое применение гаммы (если гамма короче текста).
3. Проверить работу программы на тестовом примере.

# Теоретические сведения

## Понятие гаммы

**Гамма** — это последовательность символов (или чисел), которая используется как ключевой поток. 
Если гамма короче сообщения, она повторяется циклически:

$$
K_i = K_{((i-1) \bmod \ell)+1},
$$

где $\ell$ — длина гаммы.

## Формулы гаммирования

Пусть алфавит имеет длину $n$, а символам сопоставлены коды $P_i, K_i, C_i \in \{0,\dots,n-1\}$.
Тогда:

- **шифрование:** $C_i = (P_i + K_i)\bmod n$
- **расшифровка:** $P_i = (C_i - K_i)\bmod n$

# Реализация

Ниже приведён листинг программной реализации (Julia). 
В программе используется алфавит из 33 русских букв. 
Символы, не входящие в алфавит, в данной реализации отбрасываются.

Показано в [Листинге №1](#gamma).


### Листинг 1 — Шифрование гаммированием (конечная гамма){#gamma}

```julia
# Создаём массив русских букв алфавита для шифрования
a = collect("абвгдеёжзийклмнопрстуфхцчшщъыьэюя")

# Бесконечный цикл меню
while true
    # Запрос команды и приведение к нижнему регистру
    println("ш - шифрование, р - расшифровка, в - выход")
    c = lowercase(strip(readline()))
    c == "в" && break
    c in ["ш","р"] || continue

    # Ввод сообщения
    print("Введите сообщение:")
    t = filter(x -> x in a, collect(lowercase(readline())))

    # Ввод гаммы
    print("Введите значение гамма:")
    g = filter(x -> x in a, collect(lowercase(readline())))

    # Проверка, что текст и гамма не пустые
    length(t) == 0 && length(g) == 0 && (println("Ошибка"); continue)

    # Преобразование текста и гаммы в числовые коды
    tn = [findfirst(==(x), a) for x in t]
    gn = [findfirst(==(x), a) for x in g]

    # На шифрование/расшифрование циклическое применение гаммы
    r = [c =="ш" ?
        (tn[i] + gn[(i-1) % length(gn) + 1] - 1) % 33 + 1 :
        (tn[i] - gn[(i-1) % length(gn) + 1] + 32) % 33 + 1
        for i in 1:length(tn)
    ]

    # Преобразование числовых кодов обратно в буквы и вывод результата
    println("Result: $(join([a[n] for n in r]))")
end
```


# Результаты

Результат запуска программы изображен на @fig-1.



![Результат работы программы](images/image2.png){#fig-1 width=90%}



# Выводы

1. Реализован алгоритм шифрования гаммированием с конечной гаммой.
2. Реализованы режимы шифрования и расшифровки с циклическим применением гаммы.
3. Проведён тестовый запуск, подтверждающий работоспособность реализации.


