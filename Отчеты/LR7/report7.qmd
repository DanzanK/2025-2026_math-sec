---
title: "Лабораторная работа №7"
subtitle: "Дискретное логарифмирование в конечном поле. ρ-метод Полларда"
author: "Кюнкриков Даниил Саналович (НПИмд-01-24, 1132249574)"
institute: "Российский университет дружбы народов (РУДН), Москва, Россия"
lang: ru-RU

format:
  pdf:
    toc: true
    toc-title: "Содержание"
    number-sections: true
    lof: true
    lot: true
    pdf-engine: xelatex
    fig-pos: "H"
    include-in-header:
      text: |
        \usepackage{float}
    geometry: margin=2cm
    fontsize: 12pt
  docx:
    toc: true
    number-sections: true
  html:
    toc: true
    number-sections: true

# Надёжные шрифты для Windows (кириллица)
mainfont: "Times New Roman"
sansfont: "Arial"
monofont: "Consolas"
---

# Введение

Задача дискретного логарифмирования (DLP) является базовой для многих криптографических схем с открытым ключом. В данной работе рассматривается вероятностный алгоритм ρ-метода Полларда для нахождения дискретного логарифма в мультипликативной группе конечного поля $\mathbb{F}_p$.


## Цель и задачи

Цель работы - Реализовать ρ-метод Полларда для решения задачи дискретного логарифмирования $a^x \equiv b \pmod p$ и проверить корректность реализации на тестовых данных.

Задачи:

1. Рассмотреть постановку задачи DLP в $\mathbb{F}_p$.
2. Изучить идею ρ-метода Полларда (случайное блуждание, поиск коллизии методом Флойда).
3. Реализовать алгоритм на Python и протестировать на контрольном примере.

# Теоретические сведения

## Задача дискретного логарифмирования

Пусть $p$ — простое число, $a\in\mathbb{F}_p^*$, $b\in\mathbb{F}_p^*$. Требуется найти такое целое $x$, что

$$
a^x \equiv b \pmod p.
$$

Если $r$ — порядок элемента $a$ (то есть $a^r\equiv 1\pmod p$ и $r\mid (p-1)$), то решение ищется по модулю $r$.

## Идея ρ-метода Полларда для DLP

Алгоритм строит последовательность $c_0, c_1, \dots$ в группе, где каждое состояние представимо как

$$
c_i \equiv a^{\alpha_i} b^{\beta_i} \pmod p.
$$

Переход задаётся отображением $f(c)$ (в данной реализации — разбиение по условию $c < p/2$). Для ускорения поиска коллизии используется метод Флойда (черепаха–заяц): один указатель делает 1 шаг, второй — 2 шага.

При коллизии $c_i = c_j$ получаем

$$
a^{\alpha_i} b^{\beta_i} \equiv a^{\alpha_j} b^{\beta_j} \pmod p.
$$

Подставляя $b \equiv a^x$, приходим к линейному сравнению по модулю $r$:

$$
(\beta_i - \beta_j)\,x \equiv (\alpha_j - \alpha_i) \pmod r.
$$

Его решают расширенным алгоритмом Евклида.

# Выполнение работы

## Реализация

Программная реализация расширенного алгоритма Евклида показана на [Листинге №1](#egcd).

### Листинг 1 — Расширенный алгоритм Евклида {#egcd}

```python
def egcd(a: int, b:int):
    if b == 0:
        return (a,1,0)
    g, y1, x1 = egcd(b, a % b)
    return (g, y1, x1 - (a//b) * y1)
```

Программная реализация ρ-метода Полларда для DLP показана на [Листинге №2](#rho-dlog).

### Листинг 2 — ρ-метод Полларда для дискретного логарифма {#rho-dlog}

```python
import secrets


def rho_dlog(p: int, a: int, b: int, r: int, max_steps: int = 1_000_000):
    """Ищет x из a^x ≡ b (mod p), где r — порядок a (или порядок подгруппы)."""

    half = p // 2

    def step(x: int, alpha: int, beta: int):
        """Один шаг f(x) + согласованное обновление (alpha, beta)."""
        if x < half:
            # x <- a*x
            return (a * x) % p, (alpha + 1) % r, beta
        else:
            # x <- b*x
            return (b * x) % p, alpha, (beta + 1) % r

    while True:
        # случайная инициализация
        u = secrets.randbelow(r)
        v = secrets.randbelow(r)

        c = (pow(a, u, p) * pow(b, v, p)) % p
        d = c

        alpha1, beta1 = u, v
        alpha2, beta2 = u, v

        for _ in range(max_steps):
            # черепаха: 1 шаг
            c, alpha1, beta1 = step(c, alpha1, beta1)

            # заяц: 2 шага
            d, alpha2, beta2 = step(d, alpha2, beta2)
            d, alpha2, beta2 = step(d, alpha2, beta2)

            if c == d:
                # (beta1 - beta2) * x ≡ (alpha2 - alpha1) (mod r)
                A = (beta1 - beta2) % r
                delta = (alpha2 - alpha1) % r

                g, xcoef, _ = egcd(A, r)
                if delta % g != 0:
                    # неудачный запуск — перезапустим с другими (u, v)
                    break

                r_red = r // g
                x = (xcoef * (delta // g)) % r_red
                return x

        # если по max_steps не нашли корректное решение — повторяем с новыми u,v
```

Код запуска программы показан на [Листинге №3](#main-run).

### Листинг 3 — Ввод параметров и запуск {#main-run}

```python
if __name__ == "__main__":
    print("Введите p, a, b, r через пробел (или 'выход')")

    while True:
        s = input("> ").strip().lower()
        if s == "выход":
            break

        try:
            p, a, b, r = map(int, s.split())
            x = rho_dlog(p, a, b, r)
            print("Invalid problem" if x is None else f"x = {x}")
        except Exception:
            print("Ошибка ввода. Формат: p a b r")
```

## Результаты

Проверим контрольный пример: $p=107$, $a=10$, $b=64$, $r=53$.

Ожидаемый результат: $x=20$, так как $10^{20} \equiv 64 \pmod{107}$.

Пример вывода программы:

Результат работы программы для заданных параметров показан на  @fig-1.

![Результат работы программы](images/image1.png){#fig-1 width=90%}

# Выводы

В ходе работы:

- рассмотрена постановка задачи дискретного логарифмирования в конечном поле;
- реализован ρ-метод Полларда для DLP с поиском коллизии методом Флойда;
- проверена корректность реализации на контрольных параметрах.


