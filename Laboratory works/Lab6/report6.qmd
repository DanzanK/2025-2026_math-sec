---
title: "Лабораторная работа №6"
subtitle: "Разложение чисел на множители: ρ-метод Полларда"
author:
  - "Кюнкриков Даниил Саналович (НПИмд-01-24, 1132249574)"
institute: "Российский университет дружбы народов (РУДН), Москва, Россия"
lang: ru-RU

toc-title: "Содержание"
number-sections: true

autolink-bibliography: true

format:
  pdf:
    toc: true
    lof: true
    lot: true
    pdf-engine: xelatex
    mainfont: "Times New Roman"
    sansfont: "Arial"
    monofont: "Consolas"
    fontsize: 12pt
    geometry: margin=2cm
    fig-pos: "H"
    include-in-header:
      text: |
        \usepackage{float}
  docx:
    toc: true
    mainfont: "Times New Roman"
  html:
    toc: true
---

## Цель и задачи

Цель работы - Изучить принципы работы ρ-метода Полларда для факторизации и реализовать алгоритм на языке Python.

Задачи:

1. Реализовать ρ-метод Полларда для поиска нетривиального делителя составного числа $n$.
2. Продемонстрировать работу алгоритма на тестовом примере и получить разложение $n = p\cdot q$.

## Теоретическое введение

### Постановка задачи факторизации

Дано составное число $n$. Требуется найти его нетривиальный делитель $d$, такой что $1 < d < n$.

### Идея ρ-метода Полларда

Алгоритм строит псевдослучайную последовательность по модулю $n$:

$$x_{i+1} = f(x_i) \bmod n,$$

где обычно берут $f(x) = x^2 + c$.

Для поиска «сходимости» последовательности применяется схема Флойда («черепаха–заяц»):

- $a \leftarrow f(a)$ (один шаг),
- $b \leftarrow f(f(b))$ (два шага),
- $d \leftarrow \gcd(|a-b|, n)$.

Если $1 < d < n$, то найден нетривиальный делитель $n$. Если $d = n$, попытка неудачна (обычно перезапускают с другим $c$ или стартом).

## Выполнение лабораторной работы

### Реализация алгоритма

Программная реализация ρ-метода Полларда показана на [Листинге №1](#pollard).

### Листинг 1 — ρ-метод Полларда (Python) {#pollard}

```python
import math


def pollard_rho_factor(n: int, c: int = 5, x0: int = 1) -> int | None:
    #Возвращает нетривиальный делитель n или None, если попытка неудачна

    if n % 2 == 0:
        return 2

    def f(x: int) -> int:
        return (x * x + c) % n

    a = x0
    b = x0

    # Табличный вывод : номер шага, a, b, d
    print(" i\t a\t\t b\t\t d")

    i = 0
    while True:
        i += 1
        a = f(a)          # "черепаха"
        b = f(f(b))       # "заяц"
        d = math.gcd(abs(a - b), n)

        print(f"{i}\t {a}\t {b}\t {d}")

        if 1 < d < n:
            return d
        if d == n:
            return None


if __name__ == "__main__":
    n = int(input("n = ").strip())
    d = pollard_rho_factor(n)

    if d is None:
        print("\nПопытка найти делитель провалилась (d = n).")
    else:
        print(f"\nНетривиальный делитель: {d} и {n // d}")
```

### Пример работы программы



Результат работы программы для $n=1359331$ показан на  @fig-1.

![Результат работы программы](images/image2.png){#fig-1 width=90%}

## Выводы

1. Реализован ρ-метод Полларда для поиска нетривиального делителя составного числа.
2. Продемонстрирована работа алгоритма на тестовом примере: получено разложение $1359331 = 1181\cdot1151$.


